/*
Minimal application code references. Useful for testing the engine, showcasing its features
or copying some feature usage code to get things going.

Used by the engine's root entry point application that gives a list of visual example choices to run.
*/
package example
